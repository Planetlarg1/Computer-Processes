/**
 * FOne.hdl
 * Logic drawn from turth table FOne: (f1, f0) = (1, 0).
 */

CHIP FOne {
    IN A, B, C, D; // Inputs
    OUT F, G; // Outputs

    PARTS:
    // F = A AND C
    And(a=A, b=C, out=F);

    // G = B AND D
    And(a=B, b=D, out=G);
}
