/**
 * FZero.hdl
 * Logic drawn from turth table FZero: (f1, f0) = (1, 1).
 */

CHIP FZero {
    IN A, B, C, D; // Inputs
    OUT F, G; // Outputs

    PARTS:
    // F = NOT C
    Not(in=C, out=F);

    // G = NOT D
    Not(in=D, out=G);
}